// Package api carries auctions over HTTP: it reads the JSON a publisher posts,
// offers the lot it describes to an exchange, and renders the outcome. It holds
// no rule about which partners a lot suits.
package api
